const useAddEmogy = (e?: React.MouseEvent<HTMLButtonElement>): void => {
  if (e) {
    e.preventDefault();
  }
  // addMessage({
  //   message: "",
  //   time: time,
  //   isBot: false,
  //   img: smile,
  //   isSmile: true,
  // });
  // changeIsbot(true);
};

export default useAddEmogy;
